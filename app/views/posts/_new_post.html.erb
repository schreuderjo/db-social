<%= form_for :post, url: posts_path, html: {id: "new-post"}, remote: true    do |f| %>

  <% flash.each do |key, value| %>
    <p><%= value %></p>
  <% end %>

  <p>
    <%= f.label "New Post" %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit "Post"%>
  </p>
<% end %>
